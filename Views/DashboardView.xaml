<UserControl x:Class="Lexify.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Lexify.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <Grid Background="#ECEFF4" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Başlık -->
        <TextBlock Grid.Row="0" 
                   Text="Ana Sayfa" 
                   FontSize="24" 
                   FontWeight="Bold" 
                   Margin="0,0,0,20"/>

        <!-- İstatistik Kartları -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Toplam Kelimeler -->
            <Border Grid.Column="0" 
                    Background="#5E81AC" 
                    CornerRadius="8" 
                    Margin="5" 
                    Padding="15">
                <StackPanel>
                    <TextBlock Text="Toplam Kelimeler" 
                               Foreground="White" 
                               FontSize="16"/>
                    <TextBlock Text="{Binding TotalWordsCount}" 
                               Foreground="White" 
                               FontSize="28" 
                               FontWeight="Bold" 
                               Margin="0,10,0,0"/>
                </StackPanel>
            </Border>

            <!-- Öğrenilen Kelimeler -->
            <Border Grid.Column="1" 
                    Background="#A3BE8C" 
                    CornerRadius="8" 
                    Margin="5" 
                    Padding="15">
                <StackPanel>
                    <TextBlock Text="Öğrenilen" 
                               Foreground="White" 
                               FontSize="16"/>
                    <TextBlock Text="{Binding LearnedWordsCount}" 
                               Foreground="White" 
                               FontSize="28" 
                               FontWeight="Bold" 
                               Margin="0,10,0,0"/>
                </StackPanel>
            </Border>

            <!-- Öğreniliyor Kelimeler -->
            <Border Grid.Column="2" 
                    Background="#EBCB8B" 
                    CornerRadius="8" 
                    Margin="5" 
                    Padding="15">
                <StackPanel>
                    <TextBlock Text="Öğreniliyor" 
                               Foreground="White" 
                               FontSize="16"/>
                    <TextBlock Text="{Binding LearningWordsCount}" 
                               Foreground="White" 
                               FontSize="28" 
                               FontWeight="Bold" 
                               Margin="0,10,0,0"/>
                </StackPanel>
            </Border>

            <!-- Yeni Kelimeler -->
            <Border Grid.Column="3" 
                    Background="#B48EAD" 
                    CornerRadius="8" 
                    Margin="5" 
                    Padding="15">
                <StackPanel>
                    <TextBlock Text="Yeni" 
                               Foreground="White" 
                               FontSize="16"/>
                    <TextBlock Text="{Binding NewWordsCount}" 
                               Foreground="White" 
                               FontSize="28" 
                               FontWeight="Bold" 
                               Margin="0,10,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Orta Kısım -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <!-- Son Eklenen Kelimeler -->
            <Border Grid.Column="0" 
                    Background="White" 
                    CornerRadius="8" 
                    Margin="5" 
                    Padding="15"
                    BorderBrush="#E5E9F0"
                    BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" 
                               Text="Son Eklenen Kelimeler" 
                               FontSize="18" 
                               FontWeight="Bold" 
                               Margin="0,0,0,10"/>

                    <ListView Grid.Row="1" 
                              ItemsSource="{Binding RecentWords}" 
                              BorderThickness="0"
                              Background="Transparent">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#F8F9FC" 
                                        CornerRadius="5" 
                                        Margin="0,2" 
                                        Padding="10">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="{Binding WordText}" 
                                                       FontWeight="Bold" 
                                                       FontSize="16"/>
                                            <TextBlock Text="{Binding WordType}" 
                                                       Foreground="#4C566A" 
                                                       FontSize="12" 
                                                       Margin="0,2,0,0"/>
                                            <TextBlock Text="{Binding Language}" 
                                                       Foreground="#4C566A" 
                                                       FontStyle="Italic" 
                                                       FontSize="12" 
                                                       Margin="0,2,0,0"/>
                                        </StackPanel>

                                        <Border Grid.Column="1" 
                                                Background="{Binding ColorCode}" 
                                                Width="8" 
                                                CornerRadius="4" 
                                                Margin="10,0,0,0"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </Border>

            <!-- Günün Kelimesi -->
            <Border Grid.Column="1" 
                    Background="White" 
                    CornerRadius="8" 
                    Margin="5" 
                    Padding="15"
                    BorderBrush="#E5E9F0"
                    BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" 
                               Text="Günün Kelimesi" 
                               FontSize="18" 
                               FontWeight="Bold" 
                               Margin="0,0,0,10"/>

                    <StackPanel Grid.Row="1" Visibility="{Binding WordOfTheDay, Converter={StaticResource NullToVisibilityConverter}}">
                        <TextBlock Text="{Binding WordOfTheDay.WordText}" 
                                   FontSize="22" 
                                   FontWeight="Bold" 
                                   Margin="0,0,0,10"/>

                        <TextBlock Text="{Binding WordOfTheDay.WordType}" 
                                   FontStyle="Italic" 
                                   Foreground="#4C566A" 
                                   Margin="0,0,0,5"/>

                        <TextBlock Text="{Binding WordOfTheDay.Language}" 
                                   FontStyle="Italic" 
                                   Foreground="#4C566A" 
                                   Margin="0,0,0,15"/>

                        <ItemsControl ItemsSource="{Binding WordOfTheDay.Definitions}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding DefinitionText}" 
                                               TextWrapping="Wrap" 
                                               Margin="0,0,0,5"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>